# Batch-training subset, for compute nodes with a tight home quota.
#
# Drops dash, plotly and matplotlib -- the dashboard, the MuJoCo replay viewer
# and the plotting scripts are laptop jobs and need a display anyway. Saves
# roughly 400 MB. Everything needed by train.py, calibrate_pid.py,
# confirm_advantage.py, run_seeds.py and evaluate.py is here.
#
# tensorboard stays: train.py passes tensorboard_log to PPO, and SB3 raises if
# it is missing rather than quietly skipping the logs.
#
# Consequence for the smoke check: tests/test_dashboard.py and
# tests/test_replay.py import dash and will error on collection. Run the rest:
#
#   python -m unittest tests.test_environment tests.test_calibration \
#       tests.test_callbacks tests.test_adaptation_metrics \
#       tests.test_adaptation_scenarios
#
# Install torch from the CPU index FIRST, or pip pulls ~2 GB of unused CUDA:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-server.txt

numpy==2.2.6
mujoco==3.9.0
gymnasium==1.3.0
stable-baselines3==2.9.0
torch==2.13.0
tensorboard>=2.20,<3.0
tqdm>=4.67,<5.0
rich>=14,<15
